# HZAU 校园网自动认证 —— 使用教程

> 本工具让华农校园网（认证地址 rz.hzau.edu.cn）在**开机后自动登录**，不用每次手动开网页认证。
> 只支持 Windows 10 / 11。仅供学习交流，请遵守学校网络使用规定。

---

## 一、拿到压缩包后怎么装（约 2 分钟）

1. **解压**：右键压缩包 →「全部解压」（zip 放哪都行，装好后压缩包就没用了，可删）。
2. 进入解压出来的文件夹，**双击 `install.bat`**。
   - Windows 会弹一个蓝色/黄色的 UAC 提示 → 点 **「是」**（只需这一次）。
3. 窗口提示 `请输入学号`：输入你的**学号**，回车。
4. 提示 `运营商宽带后缀`：
   - 用的是**学校校园网账号** → 直接按**回车**跳过；
   - 用的是**移动/联通/电信宽带账号** → 输入 `@cmcc` / `@unicom` / `@telecom` 后回车。
5. 提示 `请输入校园网密码`：输入你的**认证密码**（输入时不显示字符，正常现象），回车。
6. 提示 `是否现在立即认证一次以验证配置？` → 输入 `Y` 回车。
7. 看到 `认证成功` 或窗口绿色提示 **安装完成**，就装好了。按回车关闭窗口。

> 💡 装好后**重启一次电脑**，登录 Windows 后就会自动连上校园网。

---

## 二、装好后需要做什么？

**什么都不用做。** 以后：

- 每次**登录 Windows** → 自动认证，几秒内联网；
- 每 **30 分钟自动巡检**一次，断线自动重连；
- 想立刻手动验证一次：按 `Win+R`，粘贴下面路径后回车，会静默运行并写日志（约 5 秒）：

```
%LOCALAPPDATA%\HZAU-AutoLogin\hzau-autologin.exe
```

然后打开这个文件看结果（最后一行是"认证成功"即正常）：

```
%LOCALAPPDATA%\HZAU-AutoLogin\srun_login.log
```

---

## 三、日常维护

| 想做的事 | 怎么做 |
|---|---|
| **改校园网密码** | 打开 `%LOCALAPPDATA%\HZAU-AutoLogin\`，双击 `change_password.bat`，输入新密码 |
| **换电脑 / 重装系统** | 把压缩包解压后重新跑一遍 `install.bat`（密码与"本机+当前用户"绑定，换机必须重设） |
| **卸载** | 打开解压文件夹双击 `uninstall.bat`（或在管理员 PowerShell 跑 `Unregister-ScheduledTask -TaskName 'HZAU-AutoLogin' -Confirm:$false`） |

---

## 四、出问题了怎么办？

**先分清两种情况**：

### 情况 A：根本没弹出过"请输入学号" / 双击 install.bat 没反应
多半是被安全软件拦了或没解压完整 → 重新解压，右键 `install.bat` 选**以管理员身份运行**。

### 情况 B：装完了但还是上不了网
先手动验证一下到底是"工具没跑"还是"认证失败"：

1. 按上面第二章的方法手动运行一次 exe；
2. 打开日志 `%LOCALAPPDATA%\HZAU-AutoLogin\srun_login.log`，看**最后几行**：

| 日志里看到 | 说明 | 怎么办 |
|---|---|---|
| `认证成功` / `在线验证: 通过` | 一切正常 | 网络不通的话去查 WiFi 是否连的是校园网 |
| `当前已在线` | 已经在线 | 不需要处理 |
| `未找到 password.bin` / 密码相关提示 | 密码没保存好 | 运行 `change_password.bat` 重新保存 |
| `认证最终失败: ...密码错误...` | 密码/账号不对 | 确认大小写、是否多了空格；运营商账号要在安装时填 `@cmcc` 等后缀 |
| `E2620 ...超限` | 在线设备数超限 | 到 zizhu.hzau.edu.cn 自助服务，把其他设备下线 |
| `请求超时...` | 高峰期门户繁忙或网络没通 | 确认连着校园网后稍等重试 |

3. **自己看不明白？** 把压缩包里的 **`diagnose.bat` 和 `diagnose.ps1`** 复制到任意文件夹，
   **双击 `diagnose.bat`**（不要用管理员身份），它会自动收集所有排障信息并在最后生成一份报告文件；
   把**窗口完整内容**或**报告文件**发给开发者即可。

---

## 五、常见误区（重要）

- ❌ **不要直接双击解压文件夹里的 `hzau-autologin.exe`** —— 它必须在装好（跑过 install.bat）后由系统自动调用，
  直接双击会因为旁边没有配置文件而退出。想手动验证请用第二章的"安装目录里的 exe"。
- ❌ 装的时候 UAC 弹窗里**别登录别人的管理员账号** —— 配置会写进那个账号，自己登录时不会生效。
  用自己平时登录的账号确认即可。
- ⚠️ 在**非校园网环境**（家里/手机热点）开机会多等一会儿然后放弃，属正常现象，不影响使用。

---

## 六、给装机人（帮同学装的那位）

- 同学只需：**解压 → 双击 install.bat → 输学号密码**，其他别让他碰。
- 装完让同学**重启验证一次**；若失败，让他跑 `diagnose.bat` 并把报告发回来，远程就能定位。
- 密码忘记/输错不要紧：随时重跑 `install.bat` 或 `change_password.bat`。
